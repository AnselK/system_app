import { Layout } from "antd";
import React, { memo } from "react";

const SearchIndex = () => {
  return (
    <Layout.Content>
      <div className="search-index">search-index</div>
    </Layout.Content>
  );
};

export default memo(SearchIndex);
